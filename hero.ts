import { heroui } from '@heroui/theme';

export default heroui({
  themes: {
    light: {
      colors: {
        primary: {
          DEFAULT: '#016ccf',
          foreground: '#ffffff',
        },
      },
    },
  },
});
